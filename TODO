shadows are broken - the result of hit() does not look promising
everything starting at shadows is broken ...

missing: test page 159
page 161 to 166 missing


das &mut Camera in
    -) fn ray_for_pixel(c: &mut Camera, x: usize, y: usize) -> Ray;
    -) fn render(c: &mut Camera, w: &World) -> Canvas;

    entfernen und das calc_pixel im Konstruktor aufrufen

